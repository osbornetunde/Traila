import { Link } from "react-router-dom";
import styled from "styled-components";

const StyledMovieLink = styled(Link)`
  text-decoration: none;
  cursor: pointer;
`;

export default StyledMovieLink;
